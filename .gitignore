# Secrets — never commit real credentials/connection strings
.streamlit/secrets.toml
*.secrets.toml
service_account*.json

# Local database (SQLite) — data lives in the DB, not the repo
*.db
badminton.db

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.idea/
.vscode/
