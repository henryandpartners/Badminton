# --- Frontend (NiceGUI) -----------------------------------------------------
# Run: python main.py  — needs a host that runs a long-lived process
# (Render / Railway / Fly.io / VPS). The old Streamlit UI lives on the
# `claude/streamlit-legacy` branch.
nicegui>=2.0.0

# --- Data -------------------------------------------------------------------
pandas>=2.0.0

# --- Database layer ---------------------------------------------------------
SQLAlchemy>=2.0.0
# For Supabase / Postgres (not needed for the local SQLite default):
psycopg2-binary>=2.9.0
