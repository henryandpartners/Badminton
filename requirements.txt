# --- Core app ---------------------------------------------------------------
# Pinned to the range this app was tested against (verified on 1.58.0).
streamlit>=1.40.0,<1.59.0

# --- Google Sheets live backend (st.connection("gsheets", ...)) -------------
st-gsheets-connection>=0.0.4
gspread>=6.0.0
google-auth>=2.30.0

# --- Data handling ----------------------------------------------------------
pandas>=2.0.0

# --- PromptPay QR generation ------------------------------------------------
promptpay>=0.4.0
Pillow>=10.0.0

# --- Slip reading (local OCR) -----------------------------------------------
# Requires the `tesseract-ocr` system binary too — see packages.txt
# (Streamlit Community Cloud installs it from that file automatically).
pytesseract>=0.3.10
